# Contributing

Keep changes focused on SO-101 calibration, assembly animation, and Real arm setup.

Run `npm test`, `npm run build`, and `npm run audit:public` before opening a pull request.

Use synthetic calibration values in examples and bug reports.
