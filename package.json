{
  "name": "so101-helper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Small, local-first helpers for SO-101 calibration and assembly animation setup.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test",
    "audit:public": "node scripts/check-public.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
